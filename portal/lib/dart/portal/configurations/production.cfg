ENVIRONMENT = "production"

# this is the base url at which we live
APPLICATION_URL = "http://localhost:274"

# this is mounted at the root of "localhost:274", aka the APPLICATION_URL
APPLICATION_ROOT = "/dart"
